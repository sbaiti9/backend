apiVersion: v1
kind: Service
metadata:
  name: user
  namespace: {{ .Values.namespace }}
  labels:
    app: user
spec:
  type: NodePort
  selector:
    app: user
  ports:
    - name: http
      port: 80
      targetPort: {{ .Values.user.containerPort }}
      nodePort: {{ .Values.nodePorts.user }}
