apiVersion: v1
kind: Service
metadata:
  name: event
  namespace: skillio
  labels:
    app: event
spec:
  selector:
    app: event
  type: NodePort
  ports:
    - name: http
      port: 80
      targetPort: 8099
      nodePort: 30083
