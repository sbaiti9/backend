apiVersion: v1
kind: Service
metadata:
  name: entreprise
  labels:
    app: entreprise
spec:
  selector:
    app: entreprise
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP

