apiVersion: v1
kind: Service
metadata:
  name: user
  namespace: skillio
  labels:
    app: user
spec:
  selector:
    app: user
  type: NodePort
  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: 30081
