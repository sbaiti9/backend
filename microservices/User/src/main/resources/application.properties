spring.application.name=user
server.port=8085

# Eureka Client Configuration
eureka.client.service-url.defaultZone=http://localhost:8762/eureka/
eureka.instance.prefer-ip-address=true
eureka.instance.instance-id=${spring.application.name}:${random.value}

# Database Configuration (MySQL)
spring.datasource.url=jdbc:mysql://localhost:3306/entreprise_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true


# ===============================
# SECURITY (JWT Secret)
# ===============================
management.endpoints.web.exposure.include=health,prometheus,metrics
management.endpoint.prometheus.enabled=true
management.endpoint.health.show-details=when_authorized
management.metrics.tags.application=${spring.application.name}

jwt.secret=mysupersecretkeymysupersecretkey123456
jwt.expiration=86400000

# Inter-service geo events (subscription → user internal API)
skillio.internal.api-key=SkillioInternalGeo2026Key
# Admin secret for hard-freeze (JWT-independent endpoint)
liveness.admin.secret=SkillioLivenessAdmin2026SecretKey!

# SMTP (required for geo-security warning email send)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=sbaitiwissem0@gmail.com
spring.mail.password=juth dxsg dbha swyk
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Security alerts (admin recipients, comma-separated)
skilio.security.admin-alert-emails=sbaitiwissem0@gmail.com

# Points « présence » : +tick-points toutes les tick-interval-seconds (client authentifié), plafond / heure.
skillio.engagement.tick-interval-seconds=10
skillio.engagement.tick-points=10
# 360 ticks/h × 10 pts = 3600 si l’onglet envoie bien un tick toutes les 10 s pendant 1 h.
skillio.engagement.tick-max-points-per-hour=3600

# reCAPTCHA v2 (inscription). Par défaut désactivé pour éviter les 400 si le serveur n’atteint pas Google
# ou si les clés ne sont pas configurées. Mettez enabled=true + secret-key (et site key côté Angular) en prod.
# Clés de test Google (paire site/secret) : https://developers.google.com/recaptcha/docs/faq
skillio.recaptcha.enabled=false
skillio.recaptcha.secret-key=6LeIxAcTAAAAAGG-vFI1TnRWxMZNFuojJ4WifJWe

# Optional Groq AI (for admin analytics summaries)
skillio.groq.enabled=false
# skillio.groq.key=PUT_YOUR_GROQ_API_KEY_HERE
# skillio.groq.model=llama-3.3-70b-versatile