apiVersion: v1
kind: Service
metadata:
  name: training
  labels:
    app: training
spec:
  selector:
    app: training
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP

