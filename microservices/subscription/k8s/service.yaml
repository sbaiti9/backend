apiVersion: v1
kind: Service
metadata:
  name: subscription
  labels:
    app: subscription
spec:
  selector:
    app: subscription
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP

